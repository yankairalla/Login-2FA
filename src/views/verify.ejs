<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Verificação</title>
    <style>
        body {
            height: 100vh;
            width: 100vw;
            display: grid;
        }

        .box-form {
            align-self: center;
            max-width: 400px;
        }
    </style>
</head>

<body>
    <div class="container box-form .bg-light border p-3 shadow-sm">
        <form action="/verify" method="post">
            <label for="" class="form-label">Codigo de verificação:</label>
            <input type="text" name="token" class="form-control <%= validationClass ? validationClass: '' %>">
            <% if(verify===false) {%>
                <div class="invalid-feedback">
                    Token Invalido!
                </div>
                <% }else if(verify) {%>
                    <div class="valid-feedback">
                        Token valido e conta verificada!
                    </div>
                    <%}%>
                        <button type="submit" class="btn btn-primary mt-1">Enviar</button>
        </form>
    </div>


</body>

</html>